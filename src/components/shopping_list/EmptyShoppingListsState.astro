---
// Empty state component for shopping lists
---

<div class="text-center py-12">
  <div class="mb-6">
    <svg class="w-24 h-24 mx-auto text-gray-400 dark:text-gray-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M9 5H7a2 2 0 00-2 2v11a2 2 0 002 2h5.586a1 1 0 00.707-.293l5.414-5.414a1 1 0 00.293-.707V7a2 2 0 00-2-2h-2M9 5a2 2 0 002 2h2a2 2 0 002-2M9 5a2 2 0 012-2h2a2 2 0 012 2m-6 9l2 2 4-4"/>
    </svg>
  </div>
  
  <h3 class="text-xl font-semibold text-gray-900 dark:text-white mb-2">
    Noch keine Einkaufslisten vorhanden
  </h3>
  
  <p class="text-gray-600 dark:text-gray-400 mb-6 max-w-md mx-auto">
    Erstellen Sie Ihre erste Einkaufsliste, um den Überblick über Ihre Einkäufe zu behalten.
  </p>
  
  <button id="empty-create-btn" class="btn btn-success flex items-center space-x-2 mx-auto">
    <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 6v6m0 0v6m0-6h6m-6 0H6"/>
    </svg>
    <span>Erste Einkaufsliste erstellen</span>
  </button>
</div>

<script>
document.addEventListener('DOMContentLoaded', () => {
  const emptyCreateBtn = document.getElementById('empty-create-btn');
  const createBtn = document.getElementById('create-shopping-list-btn');
  
  emptyCreateBtn?.addEventListener('click', () => {
    createBtn?.click();
  });
});
</script> 