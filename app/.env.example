# Absolute base URL of the Kochbuch server API.
# Browser dev: leave EMPTY so the app uses relative /api paths proxied by Vite.
# Device/APK build: set to the server's reachable URL, e.g. http://192.168.1.20:4321
VITE_API_BASE_URL=
