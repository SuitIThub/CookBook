# Base URL for the application (Development Mode Only)
# This file is ONLY used when running 'npm run dev'
# It does NOT affect production builds (npm run build / npm run start)
# For production, use .env or .env.production
PUBLIC_SITE_URL=http://localhost:4321
